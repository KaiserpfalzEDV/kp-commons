apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mitmproxy.fullname" . }}-test-connection"
  labels:
    {{- include "mitmproxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mitmproxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
